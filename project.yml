name: "Project Tracker"
options:
  bundleIdPrefix: com.projecttracker
  deploymentTarget:
    iOS: "18.0"
settings:
  base:
    SWIFT_VERSION: "6.0"
targets:
  Project Tracker:
    type: application
    platform: iOS
    sources:
      - path: Project Tracker
    settings:
      base:
        INFOPLIST_FILE: Project Tracker/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.projecttracker.app
